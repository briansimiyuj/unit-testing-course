Let's create a package.json file to keep track of our dependencies and manage scripts. Let's install `vitest` by the following command:
   `npm i -D vitest`

`vitest` requires node 20 or higher. 

We can specify the kind of test runner we want to use in the `package.json` file. We can set the `test` script to `vitest`.

video #1