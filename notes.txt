Let's now test asynchronous function, we create `loadDeck` function that will return a promise that will resolve to a deck of cards.

To test a promise, we use the `toBeInstanceOf(Promise)` matcher. To test the resolved value of a promise, we use the `resolves.toBeDefined()` matcher.

video #8